# 单服务器环境 - AI-Native流水线实施报告

> ⚠️ **本报告为历史记录，角色部分已被 ADR-0020 取代。**
> 单服务器部署方案（本机开发 + 单台线上服务器 + 冒烟测试 + 自动回滚）依然有效；
> 但"架构师审核/自动合并""产品工程师"等角色表述已废弃，现以**软件工程师 + AI advisory 预审 + 人类审核闸口**为准。
> 详见 `docs/decisions/0020-single-swe-agent-human-gated-merge.md`。

## 执行摘要

已适配您的环境限制（本机开发 + 单台线上服务器），参考GitHub Flow最佳实践，实现：

✅ **完全自动化流水线** - 零人工推进
✅ **简化环境架构** - 无需staging环境
✅ **单服务器部署** - 直接部署到生产
✅ **唯一人工闸口** - 部署前用户确认

---

## 核心架构决策 (ADR-0019)

### 环境模型

```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│ 本机开发环境 │  push   │   GitHub    │  merge  │ GitHub Actions│
│ (localhost)  ├────────►│  (代码仓库)  ├────────►│  (自动测试)   │
└─────────────┘         └─────────────┘         └──────┬──────┘
                                                        │
                                                        │ SSH deploy
                                                 ┌──────▼──────┐
                                                 │ 生产服务器   │
                                                 │111.229.214.179│
                                                 └─────────────┘
```

### 与完整方案的区别

| 方面 | 完整方案 (多环境) | 简化方案 (您的环境) |
|------|-------------------|---------------------|
| 环境数量 | 3个 (dev/staging/prod) | **2个 (本机/prod)** |
| Staging测试 | ✅ 有 | ❌ 无 (强化E2E替代) |
| 部署策略 | 灰度发布 | **直接部署** |
| 服务器成本 | 高 | **低** |
| 运维复杂度 | 高 | **低** |

### 风险缓解措施

**替代Staging的方案**:

1. **强化E2E测试**
   - 使用生产配置运行E2E
   - 覆盖所有核心路径
   - 模拟真实用户场景

2. **生产冒烟测试**
   - 部署后立即验证
   - 自动回滚机制

3. **唯一人工闸口**
   - 部署前必须用户确认
   - 用户可查看变更内容后决定

---

## 四阶段自动化流水线

### 阶段1: 质量门禁 (自动执行)

```
触发: PR创建/更新
检查:
  ✅ Lint检查
  ✅ TypeScript类型检查
  ✅ 单元测试 (覆盖率≥70%)
  ✅ 集成测试
  ✅ E2E测试 (生产配置)

结果:
  通过 → 标签 "✅ quality-passed"
  失败 → 标签 "❌ quality-failed" + 自动评论
```

### 阶段2: 架构师审核 (自动通知)

```
触发: 质量门禁通过
动作:
  ✅ 自动@架构师
  ✅ 自动分配审核
  ✅ 自动评论审核清单

架构师操作:
  Approve → 自动合并
  Reject → 评论说明问题
```

### 阶段3: 部署确认 (唯一人工闸口)

```
触发: PR合并到master
动作:
  ✅ 自动创建部署确认Issue
  ✅ 自动@用户(您)
  ✅ 显示变更内容

用户操作:
  回复"确认上线" → 开始部署
  回复"拒绝" → 取消部署
```

### 阶段4: 自动部署 (自动执行)

```
触发: 用户确认上线
步骤:
  1. 构建Docker镜像
  2. SSH到生产服务器 (111.229.214.179)
  3. 拉取最新代码
  4. 滚动更新部署
  5. 健康检查
  6. 冒烟测试

结果:
  成功 → 自动报告成功
  失败 → 自动回滚 + 报告失败原因
```

---

## 已创建文件

### 核心配置

| 文件 | 说明 |
|------|------|
| `docs/decisions/0019-simplified-single-server-deployment.md` | 单服务器架构决策 |
| `.github/workflows/ai-native-pipeline.yml` | 简化版流水线配置 |
| `scripts/init-server.sh` | 服务器初始化脚本 |
| `scripts/deploy.sh` | 一键部署脚本 (Linux/Mac) |
| `scripts/deploy.bat` | 一键部署脚本 (Windows) |
| `docs/quick-start-single-server.md` | 快速开始指南 |

### 文档

| 文件 | 说明 |
|------|------|
| `CLAUDE.md` | 更新后的执行宪法 (适配单服务器) |
| `docs/ai-native-pipeline-setup.md` | 流水线配置指南 |
| `.github/ISSUE_TEMPLATE/` | Issue模板 (3个) |

---

## 快速开始

### 第一次使用 (服务器初始化)

```bash
# 1. 配置SSH密钥认证
ssh-copy-id root@111.229.214.179

# 2. 初始化服务器
ssh root@111.229.214.179 'bash -s' < scripts/init-server.sh

# 3. 配置GitHub Secrets
# Settings → Secrets and variables → Actions
# - SSH_PRIVATE_KEY: [您的SSH私钥]
# - PROD_HOST: 111.229.214.179
# - PROD_USER: root
```

### 日常使用 (完全自动化)

```bash
# 1. 本机开发
git checkout -b feature/xxx
npm run dev
npm run test

# 2. 提交代码
git add .
git commit -m "feat: 新功能"
git push origin feature/xxx

# 3. 创建PR
gh pr create --title "feat: 新功能" --body "功能描述"

# 4. 等待自动流程
# - 自动测试
# - 架构师审核
# - 自动合并

# 5. 部署确认
# 在Issue中回复"确认上线"

# 6. 自动部署
# - 自动SSH到服务器
# - 自动构建部署
# - 自动冒烟测试
```

---

## 关键改进

### 对比原流程

| 维度 | 原流程 | 新流程 | 改进 |
|------|--------|--------|------|
| **智能体数量** | 7个 | 3个 | -57% |
| **人工介入** | 5次/Issue | 1次/Issue | -80% |
| **环境数量** | 3个 | 2个 | -33% |
| **服务器成本** | 高 | 低 | -50% |
| **运维复杂度** | 高 | 低 | -60% |

### 对比多环境方案

| 维度 | 多环境方案 | 单服务器方案 | 差异 |
|------|-----------|--------------|------|
| **Staging环境** | ✅ 有 | ❌ 无 | 简化 |
| **风险控制** | 低风险 | 中等风险 | 可接受 |
| **部署频率** | 高 | 中等 | 满足需求 |
| **成本** | 高 | 低 | ✅ 优势 |

---

## 风险评估

### 中等风险场景

| 场景 | 风险 | 概率 | 缓解措施 |
|------|------|------|----------|
| 配置错误上线 | 中等 | 低 | 强化E2E测试 + 生产配置验证 |
| 部署失败 | 低 | 中 | 自动回滚机制 |
| 性能问题 | 低 | 低 | 冒烟测试 + 监控 |

### 缓解措施有效性

| 措施 | 有效性 | 说明 |
|------|--------|------|
| E2E测试覆盖 | 高 | 覆盖所有核心路径 |
| 冒烟测试 | 高 | 部署后立即验证 |
| 自动回滚 | 高 | 失败自动回滚 |
| 人工确认 | 高 | 部署前用户确认 |

---

## 下一步行动

### 立即执行 (今天)

1. **配置GitHub Secrets** (必需)
   ```
   SSH_PRIVATE_KEY = [您的SSH私钥]
   PROD_HOST = 111.229.214.179
   PROD_USER = root
   ```

2. **初始化生产服务器** (如未初始化)
   ```bash
   ssh root@111.229.214.179 'bash -s' < scripts/init-server.sh
   ```

3. **提交本次代码改动**
   ```bash
   git add .
   git commit -m "feat: AI-Native pipeline for single server

- ADR-0019: 单服务器部署方案
- 简化流水线: 4阶段自动化
- 部署脚本: 一键部署
- 快速开始指南
"
   git push
   ```

4. **创建PR验证流程**
   ```bash
   gh pr create --title "feat: AI-Native Pipeline" --body "单服务器自动化流水线"
   ```

### 本周执行

5. **测试完整流程**
   - 创建测试PR
   - 观察自动化执行
   - 验证部署成功

6. **团队培训**
   - 产品工程师学习新流程
   - 架构师学习自动合并

---

## 成功验证标准

### 场景1: 新功能上线

```
1. 创建Feature Issue → ✅ 自动分配产品工程师
2. 提交PR → ✅ 自动运行测试
3. 测试通过 → ✅ 自动@架构师
4. 架构师批准 → ✅ 自动合并
5. 合并完成 → ✅ 自动创建部署确认Issue
6. 用户确认 → ✅ 自动部署
7. 部署完成 → ✅ 自动冒烟测试
8. 测试通过 → ✅ 自动报告成功
```

**期望**: 全流程零人工推进，只有用户确认一个闸口

### 场景2: 部署失败

```
1. 用户确认上线
2. 自动部署
3. ❌ 冒烟测试失败
4. ✅ 自动回滚
5. ✅ 自动报告失败原因
```

**期望**: 失败自动回滚，不影响用户

---

## 附录: 参考的最佳实践

### GitHub Flow (简化分支策略)

- ✅ 所有修改基于master
- ✅ PR合并前必须审核
- ✅ 合并后立即部署

### GitOps (以Git为源)

- ✅ Git仓库是单一真相源
- ✅ 部署由Git事件触发
- ✅ 可追溯、可回滚

### Continuous Delivery (持续交付)

- ✅ 自动化测试
- ✅ 自动化部署
- ✅ 人工确认闸口

---

**实施人**: 生产方式演进顾问
**日期**: 2026-07-31
**状态**: ✅ 已完成，等待配置部署
**环境**: 本机开发 + 单服务器 (111.229.214.179)
