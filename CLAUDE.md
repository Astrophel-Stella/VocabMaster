# CLAUDE.md — AI 协作宪法

> 本文件是 AI 在本项目工作的**操作法 + 上下文入口**。所有 AI 执行必须遵守。
> 服务使命的三根支柱:**AI 执行**(怎么干)、**人类决策**(何时停下问人)、**共享上下文**(以什么为真相)。

## 1. 项目速览(共享上下文入口)

- **产品**:跨平台 AI 录音助手(录音 → 语音转文字 → AI 文本处理)。
- **技术栈**:Tauri 2 + React/TypeScript 前端直连 + Groq(默认)。Rust 后端是薄壳,业务逻辑几乎全在 `frontend/src`。
- **单一真相源**(冲突时以这些为准):
  - 需求 → `specs/requirements.md`(每条有 `REQ-ID` + 验收条件)
  - 架构 → `docs/architecture.md`
  - 关键决策 → `docs/decisions/`(ADR)
  - 构建打包 → `docs/build.md`
  - `docs/_archive/` 是**废弃方案**,严禁作为依据。

## 2. AI 执行环(怎么干一件事)

拿到一条需求或任务,按此闭环执行:

```
① 读上下文  → 读相关 REQ-ID、architecture、相关代码,确认理解
② 定方案    → 说清要改哪些文件、为什么;不确定就先问(见 §4)
③ 实现      → 写代码
④ 自检      → 跑 verify(见 §5),红了自己修到绿
⑤ 交付      → 提交/PR,说明改动 + 关联的 REQ-ID
```

**迭代上限**:同一问题连续 **3 次**改不好,**停止**,总结阻塞点,交给人决定(§4)。禁止无限打转烧 token。

## 3. 硬约定(测试与代码,不可违反)

1. **每次改完代码,必须跑 `verify`**,红了自己修到绿为止。
2. **修 bug 前先写复现测试(先红后绿)**:先写一个能重现该 bug、此刻失败(红)的测试,再改代码让它变绿。没有这个测试,不算修完。
3. **禁止奖励作弊**:不许通过删断言、写死期望值、`try/catch` 吞异常、删除/跳过测试来让 verify 变绿。
4. **改动测试文件时单独说明**改了什么、为什么(测试是判官,改判官要透明)。
5. **测试用例标注 `REQ-ID`**:如 `it('REQ-AI-001: 摘要包含原文', ...)`,以维持需求↔测试可追溯。

## 4. 人类闸口(AI 必须停下问人)

遇到以下情况,**停止并请人决策**,不得自行推进:
- 不可逆/高危操作:删数据、改生产配置、`--force`。
- 花钱或对外发布:发 Release、上架、买服务。
- 需求不清、验收条件缺失或多解。
- 视觉基线首次建立或变更(需人肉眼确认)。
- 架构级/依赖级决策(应先写 ADR,见 §6)。
- 迭代上限触发(§2)。

## 5. 常用命令

工作目录 `frontend/`:

| 命令 | 作用 |
|---|---|
| `npm run check` | **当前的 verify**:typecheck + lint + test |
| `npm run dev` | Web 开发模式(浏览器,最快验证) |
| `npm run desktop` | 桌面开发模式(Tauri 热更新) |
| `npm run desktop:build` | 打包 Windows 安装包(NSIS) |

> verify 会随基建成长:Phase 2 起加入**覆盖率 + 变异门禁**,Phase 3 加 **E2E**。届时更新本表。

## 6. 共享上下文纪律(防漂移)

- **代码变了,相关真相源要同步更新**:改了功能 → 更新 `specs/requirements.md` 的状态/锚点;改了架构 → 更新 `docs/architecture.md`;做了关键取舍 → 新增一条 ADR(`docs/decisions/`)。
- **不留第二份真相**:同一信息只在一处权威描述,别处引用它。
- **需求状态升级规则**:一条需求只有在"有通过的测试证明它满足验收条件"时,才能从 🔨 已实现 升到 ✅ 已验收。

## 7. 栈规则

- 产品代码:Rust(`src-tauri`,薄壳)+ TypeScript(`frontend/src`,业务主体)。
- 测试执行:UI/E2E 用 **Playwright(TS)**;单测用 **Vitest(TS)**;Rust 有逻辑后用 `cargo test` / `cargo-mutants`。
- 测试编排/脚本:**Python**(门禁、聚合、防漂移),放 `tooling/`。**不要**把 UI 测试或 Rust 基准硬塞进 Python。
