# 架构决策记录(ADR)

本目录用 **ADR(Architecture Decision Record)** 结构化沉淀关键决策——服务"人类决策"支柱:
让每个重要取舍**可追溯、有理由、能喂回给 AI 当上下文**。

## 约定

- 一个决策一个文件:`NNNN-短标题.md`(编号递增)。
- 决策一旦被推翻,不删除,改状态为「已废弃」并链接到取代它的新 ADR。
- 做架构级/依赖级取舍时,**先写 ADR 再实现**(见 `CLAUDE.md` §4)。

## 模板

```markdown
# NNNN. 决策标题

- 状态:提议 / 已采纳 / 已废弃(被 NNNN 取代)
- 日期:YYYY-MM-DD

## 背景
要解决什么问题?有哪些约束?

## 决策
我们决定做什么。

## 理由
为什么这样选,而不是其他方案。

## 后果
带来的好处 + 代价 + 需要注意的地方。
```

## 索引

| 编号 | 决策 | 状态 |
|---|---|---|
| 0001 | 桌面框架选 Tauri 而非 Electron | 已采纳 |
| 0002 | 前端为主,不打包 Python 后端 | **已废弃**（不适用于 VocabMaster） |
| 0003 | 默认 AI Provider 选 Groq | **已废弃**（不适用于 VocabMaster） |
| 0004 | Windows 打包用 NSIS 而非 MSI | 已采纳 |
| 0005 | 选择前后端分离架构 | ✅ 已采纳 |
| 0006 | 选择 Python FastAPI 作为后端框架 | ✅ 已采纳 |
| 0007 | 选择 SQLite 作为数据库 | ✅ 已采纳 |
| 0008 | 用户认证方案（JWT） | ✅ 已采纳 |

> **注意**：ADR-0002 和 ADR-0003 来自 AI 录音助手项目，不适用于 VocabMaster（单词学习应用）。VocabMaster 的核心决策见 ADR-0005~0008。
