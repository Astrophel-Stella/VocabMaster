# 需求规格(可追溯) v2

> 需求基建的核心产出。每条需求 = **唯一 ID + 验收条件(AC) + 状态 + 测试锚点**,
> 目的是打通「需求 → 验收条件 → 测试用例」的可追溯链路。
> 本文替代已废弃的 `docs/_archive/需求文档_v1.md`(旧版无 ID、无 AC、且技术栈已过时)。

## 状态图例

| 状态 | 含义 |
|---|---|
| 📝 规划中 | 已纳入范围,尚未实现 |
| 🔨 已实现 | 代码存在,但**尚无测试证明其符合 AC** |
| ✅ 已验收 | 有通过的测试证明其满足 AC(测试锚点已填) |

> 关键规则:**只有当一条需求的验收条件被一个通过的测试覆盖,状态才能升到「✅ 已验收」。**
> 这就是需求与测试的绑定——`已验收` 是测试基建"发"给需求的证书。

## 产品概述

跨平台 AI 录音助手:实时录音 → 语音转文字(STT)→ AI 文本处理。
平台演进:Windows 桌面 → Mac → 移动端 → 网页版。技术架构见 `docs/architecture.md`。

---

## 1. 音频录制与管理(REQ-REC)

| ID | 需求 | 验收条件(Given/When/Then) | 状态 | 测试锚点 |
|---|---|---|---|---|
| REQ-REC-001 | 实时麦克风录音 | 给定已授权麦克风,当用户点「开始录音」,则开始采集音频且 UI 进入录音态 | 🔨 已实现 | 待补(E2E) |
| REQ-REC-002 | 暂停/恢复/停止 | 给定正在录音,当点暂停则计时停止且不丢帧;点恢复从断点继续;点停止则生成完整音频 | 🔨 已实现 | 待补(`useRecorder`) |
| REQ-REC-003 | 录音时长显示 | 给定录音进行中,则界面实时显示已录时长(含暂停累计逻辑正确) | 🔨 已实现 | 待补 |
| REQ-REC-004 | 本地保存录音 | 给定录音停止,则音频 Blob 存入 IndexedDB、元数据存入 localStorage | 🔨 已实现 | 待补(`lib/db`) |
| REQ-REC-005 | 录音列表管理 | 给定已有录音,则可查看/删除/重播;删除后列表与存储同步 | 🔨 已实现 | 待补(`lib/db`/`stores`) |

## 2. 语音识别 STT(REQ-STT)

| ID | 需求 | 验收条件 | 状态 | 测试锚点 |
|---|---|---|---|---|
| REQ-STT-001 | 录音转文本 | 给定一段录音,当发起转写,则调用 Provider STT 返回文本 | 🔨 已实现 | 待补(`openaiCompatible`) |
| REQ-STT-002 | 多语言(中/英优先) | 给定中文或英文录音,则转写结果语言正确 | 🔨 已实现 | 待补 |
| REQ-STT-003 | 结果可编辑修正 | 给定转写结果,则用户可编辑并保存修改 | 🔨 已实现 | 待补 |
| REQ-STT-004 | 转写错误处理 | 给定密钥缺失/网络错误/空响应,则给出明确错误提示,不崩溃 | 🔨 已实现 | **待补(高优先,`openaiCompatible`)** |

## 3. AI 文本处理(REQ-AI)

> ⚠️ 与代码对齐后的结果:代码实际实现了 **摘要 / 关键词 / 翻译 / 润色**(`lib/aiTasks.ts`);
> 旧需求里的「内容分类/标签」代码**未实现**,标为规划中。

| ID | 需求 | 验收条件 | 状态 | 测试锚点 |
|---|---|---|---|---|
| REQ-AI-001 | 文本摘要 | 给定一段文本,当选「摘要」,则返回摘要且 prompt 含原文 | 🔨 已实现 | `lib/aiTasks.test.ts`(偏弱,待加强) |
| REQ-AI-002 | 关键词提取 | 给定一段文本,当选「关键词」,则返回关键词列表 | 🔨 已实现 | `lib/aiTasks.test.ts`(偏弱) |
| REQ-AI-003 | 翻译 | 给定文本与目标语言,当选「翻译」,则按目标语言翻译 | 🔨 已实现 | **待补:现有实现疑似忽略 language 参数(见 KNOWN-001)** |
| REQ-AI-004 | 润色 | 给定文本,当选「润色」,则返回润色后文本 | 🔨 已实现 | 待补 |
| REQ-AI-005 | 内容分类/标签 | 给定文本,则输出分类标签 | 📝 规划中 | — |
| REQ-AI-006 | 可扩展 AI 能力 | 往 `AI_TASKS` 加一项即新增一种能力,UI 自动出按钮 | 🔨 已实现 | 待补 |

## 4. 数据管理(REQ-DATA)

| ID | 需求 | 验收条件 | 状态 | 测试锚点 |
|---|---|---|---|---|
| REQ-DATA-001 | 本地存储记录 | 录音/文本/AI 结果持久化,重启后仍在 | 🔨 已实现 | 待补(`lib/db`) |
| REQ-DATA-002 | 导出 TXT/MD/JSON | 给定一条记录,可导出为三种格式 | 📝 规划中(待确认实现) | — |
| REQ-DATA-003 | 搜索(日期/内容/标签) | 可按条件筛选记录 | 📝 规划中 | — |

## 5. 用户界面(REQ-UI)

| ID | 需求 | 验收条件 | 状态 | 测试锚点 |
|---|---|---|---|---|
| REQ-UI-001 | 录音控制面板 | 主界面含开始/暂停/停止控制 | 🔨 已实现 | 待补(E2E) |
| REQ-UI-002 | 实时转录显示区 | 转写结果实时显示 | 🔨 已实现 | 待补(E2E) |
| REQ-UI-003 | 历史列表 | 展示历史记录并可操作 | 🔨 已实现 | 待补(E2E) |
| REQ-UI-004 | 设置面板 | 可配置 API 密钥、Provider、语言 | 🔨 已实现 | 待补(`settingsStore`) |

## 6. 非功能需求(NFR)

| ID | 需求 | 验收条件(可测量) | 状态 | 测试锚点 |
|---|---|---|---|---|
| NFR-PERF-001 | 录音启动 < 1s | 点击到进入录音态耗时 < 1000ms | 📝 规划中 | 待补(性能门禁) |
| NFR-PERF-002 | 识别延迟 < 5s | 转写请求返回耗时 < 5000ms(依赖 API,需定义测量条件) | 📝 规划中 | 待补 |
| NFR-PERF-003 | UI 响应 < 500ms | 关键交互响应 < 500ms | 📝 规划中 | 待补 |
| NFR-REL-001 | 异常录音自动保存 | 崩溃/中断时已录部分不丢 | 📝 规划中 | 待补 |
| NFR-SEC-001 | 密钥仅存本地 | 密钥不进网页层、不上传 | 🔨 已实现 | 待补 |
| NFR-SEC-002 | 隐私模式 | 不上传原始音频到云 | 📝 规划中 | 待补 |

---

## 已知问题(KNOWN)

| ID | 问题 | 关联需求 | 处理 |
|---|---|---|---|
| KNOWN-001 | `lib/aiTasks.ts` 的 `buildMessages` 疑似忽略 `language` 参数 | REQ-AI-003 | Phase 2「先红后绿」实战:先写复现失败测试,再修 |

## MVP 范围

**必做**:REQ-REC-001~005、REQ-STT-001、REQ-AI-001/002、REQ-UI-001~004。
**暂不做**:云同步、高级搜索(REQ-DATA-003)、协作编辑、复杂插件、分类(REQ-AI-005)。

## 追溯矩阵

自动生成的「需求 ↔ 测试」矩阵将放在 `specs/traceability.md`(Phase 5 由脚本扫描测试中的 REQ-ID 标注生成)。
约定:测试用例名或注释里标注对应 `REQ-ID`,如 `it('REQ-AI-001: 摘要包含原文', ...)`。
